# Write your query or mutation here
query Bobas($limit: Int!, $cursor: String) {
  bobas(limit: $limit, cursor: $cursor) {
    hasMore
    bobas {
      ...CommonBoba
      user {
        ...CommonUser
      }
    }
  }
}
